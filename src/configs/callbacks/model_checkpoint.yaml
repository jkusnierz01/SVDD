model_checkpoint:
  _target_: lightning.pytorch.callbacks.ModelCheckpoint
  dirpath: ${hydra:runtime.output_dir}/${logger.name}/checkpoints
  filename: "{epoch:03d}-{val_loss:.4f}"
  monitor: "val/loss"
  mode: "min"
  save_last: true